# ray
Simple Raytracer for fun

## How to build
Go into `code`.
```bash
g++ -std=c++20 -O2 raytracer.cpp -o raytracer
```

## Run
```bash
./raytracer <num samples per pixel> <resolution x> <resolution y>
```
